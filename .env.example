# The current project code does not read project environment variables.
# CDS credentials belong only in ~/.cdsapirc and must never be copied here.
# Add optional local path variables only when a future implementation consumes them.
