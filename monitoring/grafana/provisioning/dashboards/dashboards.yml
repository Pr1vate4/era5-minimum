apiVersion: 1

providers:
  - name: ERA5-Minimum
    orgId: 1
    folder: ERA5-Minimum
    type: file
    disableDeletion: true
    editable: false
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
